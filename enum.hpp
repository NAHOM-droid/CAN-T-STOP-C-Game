#pragma once
#include "tools.hpp"

enum class ColState { available, pending, captured};
enum class EColor { white, orange, yellow, green, blue};
enum class Status { begun, done, quit };
extern const string Ecolors[];
extern const string colStates[];
extern const string towerMarker[];


